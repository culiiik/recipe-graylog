greylog:
  build:
    base: 
      - go@1
    build:
      - echo `date`
    deploy: [ . ]
  run:
    prepare:
        - ls -la /var/www/
        
    init:
        - echo `date`
      
    start: sudo /var/www/run.sh
